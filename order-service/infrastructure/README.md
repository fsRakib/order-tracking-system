# Infrastructure Layer

This layer contains technical implementations that support the domain and application layers.

## Structure

- **persistence/**: Database implementations (PostgresOrderRepository)
- **messaging/**: Message queue implementations (RabbitMQ publisher/consumer)
- **grpc_client/**: gRPC client implementations (StockServiceClient)
