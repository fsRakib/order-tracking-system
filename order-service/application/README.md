# Application Layer

This layer orchestrates use cases and workflows. It coordinates domain objects and infrastructure.

## Structure

- **command/**: Command handlers for write operations (CreateOrder, UpdateOrder)
- **query/**: Query handlers for read operations (GetOrder, ListOrders)
