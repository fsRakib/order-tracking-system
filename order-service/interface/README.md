# Interface Layer

This layer handles external communication and adapts requests to the application layer.

## Structure

- **grpc/**: gRPC handlers that receive requests and delegate to application layer
